# LogWard Environment Variables
# Copy this file to .env and fill in your values

# Your server's IP address (required)
HOST_IP=192.168.1.100

# PostgreSQL password (required)
POSTGRES_PASSWORD=your-secure-password-here

# API Key Secret - generate with: openssl rand -hex 32 (required)
API_KEY_SECRET=generate-a-random-64-char-hex-string

# Fluent Bit API Key - get this from LogWard UI after first login (required for syslog)
LOGWARD_API_KEY=lp_your-api-key-from-logward-ui

# Redis Configuration (optional - only if using external Redis)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=
